diff --git a/LiveWallpaper/Components/InlineVideoPlayer.swift b/LiveWallpaper/Components/InlineVideoPlayer.swift
index 1533a87..b629330 100644
--- a/LiveWallpaper/Components/InlineVideoPlayer.swift
+++ b/LiveWallpaper/Components/InlineVideoPlayer.swift
@@ -25,6 +25,7 @@ struct InlineVideoPlayer: NSViewRepresentable {
     func makeNSView(context: Context) -> NSView {
         let view = NSView(frame: .zero)
         let player = AVPlayer(url: url)
+        player.preventsDisplaySleepDuringVideoPlayback = false
         let playerLayer = AVPlayerLayer(player: player)
         
         for track in player.currentItem?.tracks ?? [] {
diff --git a/LiveWallpaper/Components/PlayerLayerView.swift b/LiveWallpaper/Components/PlayerLayerView.swift
index 56500e3..358ef87 100644
--- a/LiveWallpaper/Components/PlayerLayerView.swift
+++ b/LiveWallpaper/Components/PlayerLayerView.swift
@@ -21,11 +21,11 @@ class CustomPlayerView: NSView {
         self.video = video
         super.init(frame: .zero)
         wantsLayer = true
-        layer?.backgroundColor = NSColor.clear.cgColor
+        layer?.backgroundColor = NSColor.black.cgColor
 
         playerLayer.player = player
         playerLayer.videoGravity = .resizeAspectFill
-        playerLayer.backgroundColor = NSColor.clear.cgColor
+        playerLayer.backgroundColor = NSColor.black.cgColor
 
         layer?.addSublayer(playerLayer)
         
diff --git a/LiveWallpaper/Singletons/UserSetting.swift b/LiveWallpaper/Singletons/UserSetting.swift
index 9400c3d..8766480 100644
--- a/LiveWallpaper/Singletons/UserSetting.swift
+++ b/LiveWallpaper/Singletons/UserSetting.swift
@@ -110,9 +110,11 @@ class UserSetting: ObservableObject {
     @Published var powerSaver = false {
         didSet {
             defaults.set(powerSaver, forKey: "powerSaver")
+            NotificationCenter.default.post(name: Self.powerSaverChangedNotification, object: nil)
         }
     }
     
+    static let powerSaverChangedNotification = Notification.Name("UserSetting.powerSaverChanged")
     static let adaptiveModeChangedNotification = Notification.Name("UserSetting.adaptiveModeChanged")
 
     @Published var adaptiveMode: Bool = false {
diff --git a/LiveWallpaper/Singletons/WallpaperManager.swift b/LiveWallpaper/Singletons/WallpaperManager.swift
index 20f157c..ea614b3 100644
--- a/LiveWallpaper/Singletons/WallpaperManager.swift
+++ b/LiveWallpaper/Singletons/WallpaperManager.swift
@@ -14,24 +14,12 @@ class WallpaperManager: ObservableObject {
     private var surfaces: [WallpaperSurface] = []
     @Published var player: AVQueuePlayer?
     
-    private var timer:Timer?
+    private var timer: Timer?
     private var didAutoPaused = false
     
     private var isPlayingBeforeSleep = false
     
     private init() {
-        timer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { [weak self] _ in
-                    // Call the function on the main thread
-            DispatchQueue.main.async {
-                if isOvercast() {
-                    self?.autoPauseVideo()
-                } else {
-                    self?.autoResumeVideo()
-                }
-            }
-        }
-        RunLoop.main.add(timer!, forMode: .common)
-        
         let workspace = NSWorkspace.shared.notificationCenter
         // Register for wake notification
         workspace.addObserver(
@@ -54,8 +42,51 @@ class WallpaperManager: ObservableObject {
             name: NSApplication.didChangeScreenParametersNotification,
             object: nil
         )
-        
+
+        NotificationCenter.default.addObserver(
+            self,
+            selector: #selector(handlePowerSaverChanged),
+            name: UserSetting.powerSaverChangedNotification,
+            object: nil
+        )
+
+        updatePowerSaverMonitoring()
     } // Singleton
+
+    deinit {
+        timer?.invalidate()
+        NSWorkspace.shared.notificationCenter.removeObserver(self)
+        NotificationCenter.default.removeObserver(self)
+    }
+
+    @objc private func handlePowerSaverChanged() {
+        updatePowerSaverMonitoring()
+    }
+
+    private func updatePowerSaverMonitoring() {
+        if UserSetting.shared.powerSaver {
+            guard timer == nil else { return }
+
+            let timer = Timer(timeInterval: 1.0, repeats: true) { [weak self] _ in
+                self?.evaluatePowerSaverState()
+            }
+            self.timer = timer
+            RunLoop.main.add(timer, forMode: .common)
+            evaluatePowerSaverState()
+        } else {
+            timer?.invalidate()
+            timer = nil
+            autoResumeVideo()
+        }
+    }
+
+    private func evaluatePowerSaverState() {
+        if isOvercast() {
+            autoPauseVideo()
+        } else {
+            autoResumeVideo()
+        }
+    }
     
     @objc private func handleWake() {
         print("System woke from sleep")
@@ -91,8 +122,8 @@ class WallpaperManager: ObservableObject {
             defer: false
         )
         
-        window.isOpaque = false
-        window.backgroundColor = .clear
+        window.isOpaque = true
+        window.backgroundColor = .black
         window.level = NSWindow.Level(rawValue: Int(CGWindowLevelForKey(.desktopWindow))) // Behind icons
         window.collectionBehavior = [.canJoinAllSpaces, .stationary, .ignoresCycle]
         window.ignoresMouseEvents = true
@@ -121,6 +152,7 @@ class WallpaperManager: ObservableObject {
         surfaces = NSScreen.screens.map { screen in
             let window = createWallpaperWindow(for: screen)
             let player = AVQueuePlayer()
+            player.preventsDisplaySleepDuringVideoPlayback = false
             let playerItem = AVPlayerItem(url: url)
             let looper = AVPlayerLooper(player: player, templateItem: playerItem)
             let playerView = PlayerLayerView(player: player, video: video)
diff --git a/LiveWallpaper/Views/VideoDropView.swift b/LiveWallpaper/Views/VideoDropView.swift
index 587cdcd..0464b0b 100644
--- a/LiveWallpaper/Views/VideoDropView.swift
+++ b/LiveWallpaper/Views/VideoDropView.swift
@@ -59,6 +59,7 @@ struct VideoDropView: View {
     private func loadVideo(from url: URL) {
         videoURL = url
         player = AVPlayer(url: url)
+        player?.preventsDisplaySleepDuringVideoPlayback = false
         player?.play()
         
         let id = UUID().uuidString
